@page "{id:int}"
@model Julero.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Goal</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Goal.name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Goal.name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Goal.startDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Goal.startDate)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Goal.endDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Goal.endDate)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Goal.ID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
